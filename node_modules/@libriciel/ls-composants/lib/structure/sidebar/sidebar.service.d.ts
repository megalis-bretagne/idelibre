import { Subject } from 'rxjs';
export declare class SidebarService {
    protected collapseSource: Subject<boolean>;
    collapse$: import("rxjs").Observable<boolean>;
    collapsed(isCollapsed: boolean): void;
}
