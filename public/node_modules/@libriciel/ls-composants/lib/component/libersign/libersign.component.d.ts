import { ElementRef, EventEmitter, NgZone, OnInit } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { LibersignCertificate } from './certificate.model';
import { LibersignUtil } from './libersign.util';
export declare class LibersignComponent implements OnInit {
    private httpClient;
    private zone;
    private element;
    constructor(httpClient: HttpClient, zone: NgZone, element: ElementRef);
    updateUrl: string;
    appletUrl: string;
    sign: EventEmitter<LibersignCertificate>;
    cancel: EventEmitter<any>;
    signed: EventEmitter<any>;
    isIE: boolean;
    isValid: boolean;
    libersignUtil: LibersignUtil;
    ngOnInit(): void;
    onSign(): void;
    onCancel(): void;
    set dataToSign(value: string[]);
    set valid(value: string);
}
