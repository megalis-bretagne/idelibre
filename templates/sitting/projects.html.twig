{% extends 'base.html.twig' %}
{% import 'macro/_forms.html.twig' as macro %}

{% block body %}
    <div class="col-8 offset-2">
        {{ apy_breadcrumb_trail_render(breadcrumb) }}
    </div>

    <div class="mt-3 col-8 offset-2">
        <h1>Gerer les projets</h1>

        <div class="mt-4">
            {{ macro.backButton('sitting_index') }}
        </div>


        <div class="row mt-4">
            <div class="col-8 offset-2 form-white">
                <button class="btn btn-primary" type="button" id="addProjectBtn"> Ajouter un projet</button>
            </div>
        </div>


    </div>

{% endblock %}


{% block javascripts %}
    <script>
        $('#addProjectBtn').click(function (event) {
            /*
            var input = document.createElement('input');
            input.type = 'file';

            input.onchange = e => {
            var file = e.target.files[0];
            }

            input.click();

             */
        })
    </script>

{% endblock %}
