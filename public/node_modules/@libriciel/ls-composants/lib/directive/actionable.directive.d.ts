import { EventEmitter } from '@angular/core';
import { HasId } from '../model/has-id';
import { ActionItem } from '../model/action-item';
export declare class ActionableDirective<T extends HasId> {
    action: ActionItem<T>;
    argument: T[];
    done: EventEmitter<any>;
    constructor();
    onClick(): void;
}
