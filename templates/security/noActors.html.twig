{% extends 'base_login.html.twig' %}

{% block body %}

    <div class="form-side">
        <div class="form-content">


            <section>
                <h3>Les élus et invités peuvent visualiser les séances via le bouton "accès aux seances" sur la page de login </h3>
                <a href="{{ path('app_logout') }}" class="btn btn-primary float-right mt-3">
                    <span class="fa fa-arrow-left"></span>
                    Retour à la page de login
                </a>
            </section>
        </div>
    </div>

{% endblock %}
