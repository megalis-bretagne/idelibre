{% if email_template is not defined or email_template.category == 'convocation' %}
    civilité de l'élu : #civilite# --
    prénom de l'élu : #prenom# --
    nom de l'élu : #nom# --
{% else %}
    civilité de l'utilisateur : #civilite# --
    prénom de l'utilisateur : #prenom# --
    nom de l'utilisateur : #nom# --
{% endif %}

{% if email_template is not defined or email_template.category != 'recapitulatif' %}
    date de la séance : #dateseance# --
    heure de la séance : #heureseance# --
    lieu de la seance : #lieuseance# --
    type de la séance : #typeseance# --
    lien vers la séance : #urlseance# --

    {% if email_template is not defined or email_template.category == 'convocation' %}
        titre de l'élu : #titre# --
    {% endif %}
    confirmation présence : #urlpresence#


{% else %}
    contenu complet du message : #recapitulatif# --
{% endif %}
