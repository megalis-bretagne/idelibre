<nav class="navbar navbar-expand-md navbar-dark bg-ls-dark">
    <a class="nav-item mr-3" href="#"><img style="height: 22px; padding-bottom: 2px;" src="{{ asset('assets/img/id-color-white.svg') }}"
                                          alt="idelibre" class="img-fluid"></a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">

            {% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                       data-toggle="dropdown"
                       aria-haspopup="true" aria-expanded="false">
                        Administration
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        {% if is_granted('ROLE_SUPERADMIN') %}
{#                            <a class="dropdown-item" href="{{ path('platform_check') }}">Services</a>#}
                            <a class="dropdown-item" href="{{ path('group_index') }}">Groupes</a>
                        {% endif %}
                        {% if is_granted('ROLE_MANAGE_STRUCTURES') %}
                            <a class="dropdown-item" href="{{ path('structure_index') }}">Structures</a>
                            <a class="dropdown-item" href="{{ path('admin_index') }}">Administrateurs</a>
{#                            <a class="dropdown-item" href="{{ path('csv_generate') }}">Utilitaires</a>#}
                        {% endif %}
                        {% if  app.user.structure %}
                            <a class="dropdown-item" href="{{ path('user_index') }}">Utilisateurs</a>
{#                            <a class="dropdown-item" href="{{ path('information') }}">Informations</a>#}
                        {% endif %}
                    </div>
                </li>

                {% if  app.user.structure %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                           data-toggle="dropdown"
                           aria-haspopup="true" aria-expanded="false">
                            Utilisation
                        </a>

                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="#">Types de séances</a>
                            <a class="dropdown-item" href="#">Thèmes</a>
                            <a class="dropdown-item" href="#">Modèles d'emails</a>
                            <a class="dropdown-item" href="#">Profile</a>
                        </div>
                    </li>
                {% endif %}

            {% endif %}
        </ul>
        {% if app.user %}
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                       data-toggle="dropdown"
                       aria-haspopup="true" aria-expanded="false">
                        {{ app.user.username }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('app_logout') }}">
                            <span class="fas fa-sign-out-alt"></span> Se déconnecter
                        </a>
                        {% if (is_granted('ROLE_MANAGE_STRUCTURES') and app.user.structure) %}
                            <div class="dropdown-divider"></div>
                            <a href="{{ path('security_impersonate_exit') }}" class="dropdown-item">
                                <span class="fas fa-sign-out-alt"></span> {{ app.user.structure.name }}
                            </a>
                        {% endif %}
                        <div class="dropdown-divider"></div>
                       {# <a href="{{ path('rgpd_policy') }}" class="dropdown-item">
                            <span class="fa fa-user-secret"></span> Politique de confidentialité
                        </a>#}

                    </div>

                </li>
            </ul>
        {% endif %}
    </div>
</nav>
