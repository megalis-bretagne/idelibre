$('document').ready(function () {
    $('select').select2({
        'closeOnSelect': true,
        'allowClear': true,
        placeholder: "Sélectionner les destinataires",
      //  'minimumInputLength': 2
    });
});
