import { HasId } from './has-id';
import { Style } from '../ui-concepts/style';
import { IActuator } from './actuator/i-actuator';
import { IValidator } from './validator/i-validator';
export declare class ActionItem<T extends HasId> {
    name: string;
    longName?: string;
    icon?: string;
    style?: Style;
    actuator: IActuator<T>;
    actionValidator?: IValidator<T>;
    enabled?: boolean;
    constructor(object: {
        name: string;
        longName?: string;
        icon?: string;
        icon_stacked?: string;
        style?: Style;
        actuator?: IActuator<T>;
        enabled?: boolean;
        fullButton?: boolean;
        actionValidator?: IValidator<T>;
    });
    initializeItem(): void;
}
