import { LibersignStatus } from './libersign-status.model';
import { LibersignCertificate } from './certificate.model';
export declare abstract class LibersignUtil {
    status: LibersignStatus;
    certificates: [LibersignCertificate];
    currentCert: LibersignCertificate;
    _favorite: LibersignCertificate;
    FAVORITEKEY: string;
    signatures: any;
    _signatures: any[];
    abstract init(updateUrl: string, appletUrl: string): any;
    abstract loadCertificates(): any;
    abstract selectCertificate(cert: LibersignCertificate): any;
    abstract doSign(dataToSign: string[]): any;
    abstract destroy(): any;
    constructor();
    set favorite(cert: LibersignCertificate);
    get favorite(): LibersignCertificate;
    sign(dataToSign: string[]): void;
    commonLoadCert(): void;
}
